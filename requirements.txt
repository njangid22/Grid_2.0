# Essential for your Django app
Django>=3.2,<4.0
gunicorn  # You’ll need this for running Django on Vercel or any other deployment
psycopg2-binary  # This replaces psycopg2 for easier installation
whitenoise  # To serve static files efficiently

# Useful utilities and dependencies for web apps
requests==2.32.3
sqlparse==0.5.1




# Lighter replacements for heavy libraries
opencv-python-headless==4.10.0.84  # Lighter version of OpenCV, no GUI
tensorflow-cpu==2.17.0  # Use CPU version, significantly smaller than full TensorFlow
numpy==1.26.4  # Core scientific library

# Remove all Jupyter-related libraries, as they are not needed in production
# Development tools, these are excluded:
# ipython, jupyter, jupyterlab, etc.

# Replace any large or non-production libraries
# Removed: tensorflow_object_detection_api, h5py, ipywidgets, etc.

# Optional: If you're using authentication
django-allauth==0.55.0

# Optional: If you're using Django REST Framework
djangorestframework==3.15.0
